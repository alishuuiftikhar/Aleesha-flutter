# Translation with AI (dart script: translate_script.dart)

Copy the .env file to the root folder of flux_localization (not the scripts folder) from the scripts/.env.example file and fill in the values ​​there.

Then open terminal in flux_localization and run the following command:

```bash
dart ./scripts/translate_script.dart
```
